using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FlockManager : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//take 3d models, replicate them multiple timmes
	}
	
	// Update is called once per frame
	void Update () {
		//loop through models call enscary method depending on models current state.
        //each model has its own update method?

        //parent class loops through models?

        //or each model has its own class
        //in which case this class sets up the other models

        //rename to FLockManager?


        //write indivudal methods first
        //then worry about how best to manage them

	}


   


}
